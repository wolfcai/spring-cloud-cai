spring:
  application:
    name: cai-order
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  main:
    #springboot 2.6.0后默认禁止循环依赖
    allow-circular-references: true


